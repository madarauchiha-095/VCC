{
  "name": "event-management-system",
  "version": "1.0.0",
  "description": "Institutional Event Resource Management System - Full Stack",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test.js",
    "test:conflicts": "node test-conflicts-only.js",
    "test:watch": "nodemon test.js",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm install",
    "build:frontend": "cd frontend && npm install && npm run build",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "jsonwebtoken": "^9.0.0",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "axios": "^1.13.5",
    "concurrently": "^8.0.0",
    "nodemon": "^3.0.0"
  }
}
